import PlaceholderPage from "@/components/admin/PlaceholderPage";
import { FileText } from "lucide-react";

export default function AdminRequestsPage() {
  return <PlaceholderPage title="בקשות" icon={FileText} />;
}
