import PlaceholderPage from "@/components/admin/PlaceholderPage";
import { Settings } from "lucide-react";

export default function AdminSettingsPage() {
  return <PlaceholderPage title="הגדרות" icon={Settings} />;
}
